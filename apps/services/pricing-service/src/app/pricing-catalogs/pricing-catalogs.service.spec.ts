import { PricingCatalogsService } from './pricing-catalogs.service';

describe('PricingCatalogsService', () => {
  it('can be instantiated', () => {
    const service = new PricingCatalogsService();

    expect(service).toBeDefined();
  });
});
